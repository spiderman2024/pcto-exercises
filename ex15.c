#include <stdio.h>

int sconto (int articoli, int prezzo) {

}

int main () {

   int articoli = 1;
   int prezzo;
   int prezzoscontato;

   prezzo = 5;
   printf("Inserire numero articoli\n");
   scanf("%d", &articoli);
   while(articoli > 0) {
      printf("Inserire numero articoli\n");
      scanf("%d", &articoli);
   if(0 < articoli < 30); {
   printf("Il prezzo è %d * %d\n", articoli, prezzo);
   else if(30 <= articoli < 50); {
   printf("Il prezzo scontato è %d * %f\n", articoli, prezzoscontatoprimo);
   else if(articoli >= 50); {
   printf("Il prezzo scontato è %d * %f\n", articoli, prezzoscontatosecondo);
   }
   }
   return 0;
   }
   }